# Worker Node

Lightweight Go worker node daemon for storing encrypted chunks and responding to proof-of-storage challenges.

Build & run:

```bash
go build ./...
./worker_node
```

Notes:
- This worker node is a lightweight Go daemon for encrypted chunk storage and proof-of-storage endpoints.
